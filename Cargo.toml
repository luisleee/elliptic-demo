[package]
name = "elliptic"
version = "0.1.0"
edition = "2021"

[dependencies]
criterion = {version = "0.7", features = ["html_reports"]}

[[bench]]
name = "field"
harness = false

[[bench]]
name = "point"
harness = false